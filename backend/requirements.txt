# Ellis visa backend — production dependencies.
# Pinned loosely; CI resolves exact versions.
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
httpx>=0.26
python-multipart>=0.0.9
# --- Live stack drivers (required for the docker-compose full stack) ---
psycopg[binary]>=3.1          # PostgreSQL driver (compose api+worker)
temporalio>=1.5               # durable workflows against the Temporal cluster
# --- Activated per-integration (import-guarded; absent → local fallback) ---
alembic>=1.13
jsonschema>=4.21
# boto3>=1.34                 # S3 / KMS / Secrets Manager
# google-cloud-documentai     # OCR
# stripe>=8                   # Issuing
# docusign-esign              # embedded signing
# sentry-sdk / opentelemetry  # observability
google-auth>=2.20
requests>=2.31
pytest-asyncio>=0.23
pycountry==26.2.16

# Live Browserbase runtime driver + credential-free recon (optional; needs
# `python -m playwright install chromium`). Live paths are flag/creds-gated.
playwright>=1.44
Pillow>=10.0
pypdf>=4.0
pypdfium2>=4.30
