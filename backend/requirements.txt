# Ellis visa backend — production dependencies.
# Pinned loosely; CI resolves exact versions.
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
httpx>=0.26
python-multipart>=0.0.9
# --- Activated per-integration (import-guarded; absent → local fallback) ---
# psycopg[binary]>=3.1        # PostgreSQL / Neon driver
# alembic>=1.13               # migrations (create_all is used when absent)
# temporalio>=1.5             # durable workflows (DB runner used when absent)
# boto3>=1.34                 # S3 / KMS / Secrets Manager
# google-cloud-documentai     # OCR
# stripe>=8                   # Issuing
# docusign-esign              # embedded signing
# sentry-sdk / opentelemetry  # observability
google-auth>=2.20
requests>=2.31
pytest-asyncio>=0.23
