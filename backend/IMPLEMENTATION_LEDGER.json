{
  "updated": "2026-07-23 checkpoint (execution-classification foundation: MOCK never presented as real)",
  "completed_2026_07_23_execution_classification": [
    "ABSOLUTE REQUIREMENT — execution classification: NEW app/execution.py defines ExecutionClass (exactly the brief's 7: MOCK, LOCAL_PROVIDER, LIVE_SANDBOX, LIVE_PRODUCTION, APPLICANT_ACTION_REQUIRED, MANUAL_REVIEW_REQUIRED, UNSUPPORTED). Classification is DRIVER-DETERMINED and clamped: a result can never be classified more-real than the driver that produced it, so a MockPortal-backed adapter that CLAIMS production still classifies MOCK (structurally impossible to convert a mock into a production result). classify_driver/classify_adapter/classify_ocr + clamp + is_real_government_result (True only for LIVE_PRODUCTION) + result_disposition (the display guard) + assert_not_mock_in_production + legend + record_execution (persists to NEW models.ExecutionRecord + audit).",
    "WIRING: MockPortal.execution_class='MOCK' (truthful self-declaration). StoredDocument gains execution_class + page_classification columns. OCR endpoint classifies (LOCAL_PROVIDER with no cloud creds; LIVE_PRODUCTION for Document AI/Kimi vision), persists on the doc + ExecutionRecord + audit, returns execution_class. GET /cases/{id} returns execution_class + a disposition block (is_real_government_result/display_status/disclaimer) and stamps appointment/confirmation with is_real flags. /capabilities exposes the legend + runtime_portal_execution_class='MOCK' + production_mode. Case-completion records a case_completed ExecutionRecord (MOCK). config.production_mode (env in production|staging).",
    "FRONTEND DISPLAY GUARD: lib/visaSession.resultDisposition() mirrors the backend, FAIL-SAFE (missing disposition => NOT real). CaseFlow.jsx: persistent ExecutionBanner on the journey tab + ResultView refuses to render 'Application submitted'/'Reference number' as real unless disposition.is_real_government_result — otherwise labels 'Mock reference (not a real visa reference)' + disclaimer. This satisfies 'the production UI must refuse to display submitted/paid/booked/confirmed unless the approved live adapter verified it'.",
    "TESTS: tests/test_execution.py (17: enum shape, driver classification, clamp cannot upgrade mock->prod, adapter-claims-prod-but-mock-driver=>MOCK, OCR local vs live, only LIVE_PRODUCTION is real, completed-mock-never-real, assert guard, capabilities legend, OCR endpoint persists LOCAL_PROVIDER, get_case MOCK disposition, completed-case confirmation not-real + ExecutionRecord). tests/visa/visa_ui_logic.test.mjs +3 (mock not real, missing=>not real fail-safe, live-prod real). Backend hermetic 78 (was 61); npm 48 (was 45)."
  ],
  "resume": {
    "how_to_continue": "Everything needed is in this repo + ledger + tests + commit history. Recreate venv: (cd backend && python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt). Real creds live ONLY in the git-ignored backend/.env (not committed) and host ADC at ~/.config/gcloud/application_default_credentials.json. Private passport fixtures live ONLY at ~/.ellis_private_fixtures (0600, outside repo). Bring up the full stack: (cd backend && docker compose up -d) — 8 services, all healthy. Run the app: npm run dev.",
    "done_this_session": "BRIEF-3: verified rotated Moonshot key (backend/.env only, text+vision+module, no leak). Phase 1 clean Electron build + hardened package secret scan + scripts/scan-release-secrets.sh + CI gate (.github/workflows/security.yml) + docs/ARTIFACT_SECURITY_REPORT.md (commit b372358). Phase 2 country-adapter admin + approval lifecycle (app/adapters_admin.py + models + /admin/adapters endpoints + AdminConsole.jsx, human-only activation, no AI activation, immutable audit, rollback, coverage matrix; 8 tests) (commit acad255). Phase 11 Trip.com IT delivery package (README_TRIPCOM_TESTING.md + acceptance plan + scripts/*.sh; run-acceptance-tests.sh => PASS).",
    "next_implementation_task": "BRIEF-3 remaining, in order: PHASE 6 Trip.com administration UI + webhook replay viewer + integration health (reuse app/integrations/tripcom.py) with a TripcomAdmin.jsx screen; PHASE 8 Sentry + OpenTelemetry + expanded /metrics + structured REDACTED request-logging middleware + redaction tests; PHASE 9 configurable load tests (EXPECTED_* env) + report; PHASE 7 staging/prod deploy IaC (no secrets); PHASE 10 full Playwright-recorded graphical demo; PHASE 3/4 real Playwright portal drivers per approved adapter (safe examples only until Trip.com priorities).",
    "next_file_to_modify": "backend/app/observability.py (NEW: structured redacted logging + OTel/Sentry init behind flags) + wire into app/main.py; then src/renderer/src/screens/TripcomAdmin.jsx (tenants/roles/deep-links/webhook replay/health) reusing app/integrations/tripcom.py; add tests/test_observability_redaction.py.",
    "next_command_to_run": "./scripts/start-local.sh && ./scripts/run-acceptance-tests.sh   # stack up + PASS; then build the observability middleware",
    "verify_commands": [
      "cd backend && . .venv/bin/activate && python -m pytest tests/test_unit.py tests/test_e2e.py tests/test_signature.py tests/test_discovery.py tests/test_tripcom.py tests/test_temporal.py tests/test_privacy.py -q   # 53 pass",
      "npm test   # 43 pass (6 security + 11 visa + 26 portal)",
      "cd backend && docker compose up -d && LIVE_TEMPORAL=1 .venv/bin/python -m pytest tests/test_live_temporal_restart.py -s   # live worker-restart durability",
      "node --test tests/visa/frontend_backend.test.mjs   # frontend->backend full lifecycle (needs stack up)",
      "cd backend && . .venv/bin/activate && python -m pytest tests/test_live_providers.py -q   # needs backend/.env creds",
      "cd backend && . .venv/bin/activate && ENABLE_PRIVATE_DOCUMENT_SMOKE_TESTS=true PRIVATE_PASSPORT_FIXTURE_US=~/.ellis_private_fixtures/fixture_us.jpg (+SG/SG_PDF/CN/CN_2/PDF) python -m pytest tests/test_private_passports.py -q"
    ]
  },
  "test_status": {
    "js_framework": {"suite": "node --test tests/portal/", "result": "26 passed"},
    "backend_full": {"suite": "pytest test_unit+test_e2e+test_signature+test_discovery+test_tripcom+test_temporal", "result": "45 passed"},
    "live_providers": {"suite": "pytest tests/test_live_providers.py", "result": "3 passed (kimi text, kimi vision, browserbase) — needs .env creds"},
    "private_passport_smoke": {"suite": "pytest tests/test_private_passports.py (flag-gated)", "result": "all 6 fixtures via Document AI OCR+Form Parser; 6/6 MRZ checksum valid, high confidence"}
  },
  "providers": {
    "kimi_k3":        {"status": "provider_tested",       "notes": "live text + vision; reasoning_content fallback + big token budget"},
    "google_document_ai": {"status": "provider_tested",  "notes": "ADC via google-auth; OCR + Form Parser HTTP 200; real passport MRZ checksum VALID; quota project quiet-terminal-503221-i3; processors in project 712137185724"},
    "browserbase":    {"status": "provider_tested",       "notes": "session create/release live; live_view_url wired"},
    "postgres":       {"status": "integration_tested",    "notes": "compose up healthy; schema applied clean; full workflow + restart durability on Postgres; BigInteger fix for epoch-ms"},
    "docker":         {"status": "integration_tested",    "notes": "FULL compose stack up + healthy: postgres, temporal(+ui), minio, mailpit, api, worker, mock-portal. Dockerfile drops tests COPY; requirements add psycopg[binary]+temporalio; every service has healthcheck + startup ordering; ADC mounted read-only into api+worker only. Containerized API creates cases on Postgres."},
    "stripe_issuing": {"status": "implemented_untested",  "notes": "applicant payment window is the working default"},
    "clerk":          {"status": "implemented_untested",  "notes": "dev-token auth active; verify_clerk stubbed"},
    "temporal":       {"status": "integration_tested",    "notes": "REAL Temporal cluster (temporalio/auto-setup 1.24) live in compose; worker (app.temporal_worker) connects with a DB-backed portal store (portal_states table) so reconciliation survives restart. tests/test_live_temporal_restart.py (LIVE_TEMPORAL=1) drives a case to a human-action state, STOPS the worker container (Temporal describe=RUNNING → durable), RESTARTS it, resumes via signals, completes, and proves charge/book/submit each == 1 across TWO restarts + reconcile short-circuit. In-process time-skipping tests still 2 pass."}
  },
  "ocr_hierarchy": {
    "tier1_document_ai": "primary, provider_tested on consented passport",
    "tier2_kimi_vision": "flag-gated ENABLE_KIMI_OCR_FALLBACK, image-only, records fallback, provider_tested reachability",
    "tier3_local_mrz": "deterministic ICAO parse+checksum, unit_tested",
    "conflict_policy": "checksum-valid deterministic MRZ preferred; disagreements -> applicant review (cross_document_conflicts)"
  },
  "features": {
    "case_api": "unit_tested", "document_upload_ocr": "provider_tested", "applicant_review_approve": "unit_tested",
    "durable_workflow": "integration_tested", "worker_restart_durability": "integration_tested",
    "kimi_tool_security": "unit_tested", "tenant_isolation": "unit_tested", "secret_redaction": "unit_tested",
    "appointment_engine": "unit_tested", "portal_adapters_mockland_vietnam": "unit_tested",
    "applicant_payment_window": "unit_tested (descriptor)", "ocr_health_diagnostic": "provider_tested"
  },
  "private_fixtures": {
    "location": "~/.ellis_private_fixtures (0700, files 0600, OUTSIDE repo, gitignored, dockerignored)",
    "recorded": "sha256 only; no personal values anywhere",
    "env_flags": ["ENABLE_PRIVATE_DOCUMENT_SMOKE_TESTS", "PRIVATE_PASSPORT_FIXTURE_PDF/US/SG/CN"]
  },
  "completed_2026_07_22b": [
    "PHASE 1 native e-signature: app/providers/esign.py (ESignatureProvider + NativeEllisSignatureProvider) + pdfgen.py (dep-free signed PDF) + models NativeSignature/SignatureEvent + /authorization/prepare + /authorization/sign (consent+intent+step-up token gates) + tamper-evident artifact hash + material-change invalidation + cross-tenant block. tests/test_signature.py 6 pass.",
    "PHASE 5 portal discovery: app/portal/discovery.py (query variants + official-domain classification + disabled-draft-only) + Kimi allowlist tool discover_official_visa_portal + models PortalDraft + /discovery + /discovery/drafts + review endpoint. tests/test_discovery.py 5 pass. Live Browserbase search = activation step (returns honest 'unavailable', never fabricates).",
    "PHASE 9 Trip.com sandbox: app/integrations/tripcom.py (HMAC signing ts.nonce.body, replay protection, idempotency, typed webhook payloads, SandboxClient, deep-link) + docs/TRIPCOM_REQUIREMENTS.json (machine-readable what-Tripcom-must-provide). tests/test_tripcom.py 6 pass."
  ],
  "test_totals_now": {"js": "26", "backend_hermetic": "45 (incl. 2 real-temporal)", "live_providers": "3", "private_passport": "all 6 fixtures processed via Document AI, 6/6 MRZ checksum valid"},
  "completed_2026_07_22c": [
    "PRIVATE FIXTURES: all 6 consented passports (US/SG jpg+pdf, CN x2 jpg, PDF) processed through REAL Google Document AI (Enterprise OCR + Form Parser both ok on all 6); after MRZ parser hardening (regex search-anywhere in _find_td3_lines + _repair_numeric OCR fix for O/0/I/1 on date+checkdigit fields) all 6/6 classify as passport with deterministic ICAO MRZ checksum VALID, high confidence, 0 warnings, 0 conflicts. Structural-only reporting; no personal values. Chinese-passport MRZ (line1 'PO'+CHN, OCR noise) now validates.",
    "PHASE 2 REAL TEMPORAL: temporalio 1.30.0 installed (py3.14); app/temporal_app.py (VisaProcessingWorkflow @workflow.defn + 8 @activity.defn wrapping mock portal; signals approve_review/sign_authorization/solve_captcha/verify_email/approve_payment/complete_payment/complete_declaration; pay/book/submit activities RECONCILE via get_application_state before acting); app/temporal_worker.py entry point for TEMPORAL_HOST; docker-compose worker -> app.temporal_worker. tests/test_temporal.py 2 pass using WorkflowEnvironment.start_time_skipping(): drives to COMPLETED via signals, asserts no dup pay/book/submit, and Replayer.replay_workflow(history) proves determinism = restart-safe. pytest.ini asyncio_mode=auto."
  ],
  "completed_2026_07_23_security_phase1": [
    "SECURITY CORRECTION: removed the bundled Kimi key from the Electron client. Confirmed shipped inside release/mac-arm64/Ellis.app/Contents/Resources/app.asar (/resources/kimi.key) → marked for rotation in docs/SECURITY_ROTATION.md. src/main/index.js no longer reads any bundled/drop-in credential file; all client-side external-provider calls gated behind clientProviderCallsAllowed()=!app.isPackaged (packaged app = zero direct provider calls; everything backend-only via visaBackend.js). package.json build.files hardened with negation globs. Deleted leaked key + compromised release. Added tests/build_security.test.mjs (in npm test): fails on any provider key/private fixture/.env/ADC/credential resource in a shippable dir or inside a packaged app.asar. npm test 32 pass (6 security + 26 portal). Client error messages redact secrets.",
    "PHASE 1 LIVE DOCKER + TEMPORAL: full compose stack (postgres, temporal+ui, minio, mailpit, api, worker, mock-portal HTTP service app/portal/http_mock.py) with healthchecks + correct depends_on ordering; temporal healthcheck uses service-DNS tctl. DB-backed portal store (app/portal_store.py + models.PortalState) makes reconciliation restart-safe. Proved live worker stop/restart durability + no duplicate pay/book/submit via tests/test_live_temporal_restart.py."
  ],
  "completed_2026_07_23_applicant_ui": [
    "PHASE 2-7 APPLICANT UI: full graphical applicant workflow in the Electron renderer, wired to the backend via src/renderer/src/lib/visaBackend.js over authenticated HTTP (NEVER providers directly). New nav 'Visa Platform' -> screens/VisaConsole.jsx (session, new case, case list, honest capability + country matrix) mounting components/visa/CaseFlow.jsx which reads workflow status and resolves each backend handoff by signal. Components: OcrReview.jsx (drag/drop upload, MIME/size validation, Document-AI status, per-field accept/edit/reject with confidence+provenance+MRZ-conflict highlight, missing-info step, approve-canonical -> signature invalidation feedback), Preferences.jsx (full appointment prefs incl. auto-book/reschedule controls), handoffs.jsx (SignatureModal typed/drawn + consent/intent/step-up + document hash; LiveViewModal captcha/email/otp/identity — Ellis never solves CAPTCHAs, collects no passwords/OTP/card/CAPTCHA answers; PaymentApprove + PaymentModal applicant-controlled, card never seen; AppointmentCalendar slot pick; RescheduleConfirm; DeclarationModal — only the applicant signs). Pure logic in lib/visaSession.js.",
    "BACKEND UI-SUPPORT: CORS middleware (env ELLIS_CORS_ORIGINS) so the renderer can reach the API; getCase now returns appointment+confirmation; review returns required_fields/missing_fields/answers; new POST /cases/{id}/answers (missing-info, invalidates signatures); mock-only GET /cases/{id}/mock/verification for the email-verification demo (dev-gated, no PII).",
    "BUGFIX (durability): workflow._last_slots was not persisted across the per-signal reload, so APPOINTMENT_AVAILABLE falsely reported no availability after select_appointment when allowAutoBook=false. Now persisted in snapshot/restore. Surfaced by the new frontend->backend test.",
    "TESTS: tests/visa/visa_ui_logic.test.mjs (11 hermetic, in npm test) + tests/visa/frontend_backend.test.mjs (drives the REAL visaBackend.js client through create->upload->OCR->review->missing-info->preferences->prepare+sign->start->every handoff->COMPLETED against the live API; auto-skips if backend down). npm test now 43 pass (6 security + 11 visa + 26 portal); backend hermetic still 45; frontend->backend live PASS.",
    "GRAPHICAL DEMO: launched electron-vite dev (app boots clean: main+preload+renderer, no errors) and rendered the real Visa Platform screen in a browser via a safe window.ellis fallback (graceful degradation when the preload bridge is absent; never used in the real Electron app). Screens verified visually. Backend fetch from the preview sandbox is blocked (sandbox can't reach host localhost:8000) — that connectivity is proven instead by the host-side frontend->backend test."
  ],
  "completed_2026_07_23_privacy_ops": [
    "PHASE 10 PRIVACY: app/privacy.py + endpoints — GET /cases/{id}/export (portable secret-free bundle: case/applicant/documents/signatures/appointments/confirmations/audit), GET /export (tenant), DELETE /cases/{id} (case erasure: cascades every per-case table + portal_states + old audit, leaves a non-PII 'case_erased' tombstone; orphaned applicant erased), DELETE /applicants/{id} (right-to-erasure across all their cases). retention_due() lists cases past ELLIS_RETENTION_DAYS. UI: PrivacyPanel in the case Activity tab (export JSON download + confirm-gated erasure) + visaBackend client methods.",
    "PHASE 11 OPS: GET /readyz (API + DB reachability), GET /metrics (org-scoped, no PII: cases_by_state + appointment/submission/signature counts + capabilities + kill_switches), config.killswitches() (env ELLIS_KILL_KIMI/DOCUMENT_AI/BROWSERBASE/STRIPE/SUBMISSIONS + ELLIS_KILL_ADAPTERS) now gate the capability flags so an operator can disable a provider/adapter instantly without redeploy.",
    "TESTS: tests/test_privacy.py 8 pass (export completeness+secret-free, tenant-isolated export/delete 403, cascade+tombstone, applicant erasure, readyz, org-scoped metrics, kill-switch env). Backend hermetic now 53. Live smoke: /readyz + /metrics + frontend->backend regression all green."
  ],
  "completed_2026_07_23_brief3": [
    "NEW KIMI KEY: rotated Moonshot key placed ONLY in git-ignored backend/.env; verified FULLY working (real text + vision + provider-module calls) with no value printed/committed/packaged (fingerprint sha256[:12]=6b9bca47f632).",
    "BRIEF-3 PHASE 1 CLEAN BUILD: rm -rf out release; electron-vite build + electron-builder --dir; verified no resources/kimi.key, no credential/.env/ADC/fixture in app.asar, no secret VALUES in packaged contents, packaged main enforces backend-only (clientProviderCallsAllowed, 5 sites). scripts/scan-release-secrets.sh (extract+scan asar, redacted findings, CI exit code) + npm run scan:release. build_security.test.mjs strengthened (deep asar content scan + packaged-main guard + MOONSHOT/BROWSERBASE/LiveView patterns) → 8 tests. .github/workflows/security.yml release gate. docs/ARTIFACT_SECURITY_REPORT.md. commit b372358.",
    "BRIEF-3 PHASE 2 ADAPTER ADMIN LIFECYCLE: app/adapters_admin.py (states discovered→disabled_draft→technical_review→policy_review→mock_tested→staging_tested→approved→limited_rollout→production_active→paused/rolled_back; ALLOWED_TRANSITIONS; ACTIVATION_STATES human-admin-only + NO AI actor may activate; immutable audit per transition; production_enabled only in rollout states & forced off by kill switch; version snapshots + rollback; coverage_matrix with honest service levels). models AdapterRecord+AdapterVersion. security.Principal gains role + require_admin (admin granted only by ELLIS_ADMIN_TOKEN, dev default 'admin-token'). Endpoints /admin/adapters (CRUD/transition/kill/clear-kill/rollback/coverage). UI screens/AdminConsole.jsx (list, detail w/ lifecycle+versions+rollback+kill, review queue, coverage matrix, immutable audit) + nav. tests/test_adapters_admin.py 8 pass (lifecycle, illegal-transition, activation-requires-admin, AI-cannot-activate, kill, config-frozen-after-approval, rollback, coverage). backend hermetic now 61; npm test 45. commit acad255.",
    "BRIEF-3 PHASE 11 DELIVERY PACKAGE: README_TRIPCOM_TESTING.md (prereqs, one-command startup, dev login accounts admin/1234 + dev-token/admin-token, mock creds, full scenario, troubleshooting, shutdown, rotation, known limitations) + docs/TRIPCOM_ACCEPTANCE_TEST_PLAN.md (27 acceptance tests AT-01..AT-27 with id/preconditions/steps/expected/evidence/result/notes + honest gaps) + scripts/{setup-local,start-local,health-check,run-acceptance-tests,stop-local,reset-local,build-electron-clean,scan-release-secrets}.sh (fail-safe, never print secrets) + ELLIS_ADMIN_TOKEN in backend/.env.example. run-acceptance-tests.sh => ACCEPTANCE: PASS (npm 45, backend 61, frontend->backend live, release scan CLEAN). health-check.sh => all 6 services OK."
  ],
  "not_done_yet": [
    "BRIEF-3 PHASE 3 global portal runtime (Browserbase/Playwright/versioned drivers/Stagehand-Kimi-assist-only/Temporal/human-takeover/idempotency/reconciliation) — runtime skeleton exists (temporal_app + mock portal); real Playwright drivers per approved portal remain.",
    "BRIEF-3 PHASE 4 real adapter rollout pipeline (safe examples only until Trip.com priorities), PHASE 5 payments/appointments polish, PHASE 6 Trip.com admin UI + webhook viewer + health, PHASE 7 cloud deploy IaC (staging/prod), PHASE 8 Sentry+OTel+metrics+alerts+redaction tests, PHASE 9 load tests+report, PHASE 10 full graphical demo recording, PHASE 11 Trip.com IT delivery package (README_TRIPCOM_TESTING.md, scripts/*, acceptance plan).",
    "PHASE 3/4/5(UI)/7(UI)/8-of-second-brief: full Electron applicant UI screens (Live View modals, payment modal, appointment calendar, OCR-review UI, signature UI) wired to backend via src/renderer/src/lib/visaBackend.js — DONE in checkpoint 05c849f (superseded).",
    "PHASE 7 full country-adapter build pipeline beyond discovery drafts + Vietnam config (real Playwright drivers per portal)",
    "PHASE 9 production multi-tenant storage (S3/GCS/KMS/Secret Manager), data export/deletion endpoints",
    "PHASE 10 monitoring (Sentry/OTel/metrics/alerting/kill-switches), load tests, staging deploy",
    "Stripe Issuing live, synthetic passport-layout fixtures, Electron end-to-end UI tests"
  ],
  "next_actions": [
    "PHASE 2: add temporalio to requirements + docker-compose temporal/temporal-ui services; create app/temporal_workflows.py mirroring workflow._step as activities behind TEMPORAL_HOST; keep DB runner for unit tests; prove kill/restart no-dup with a real worker.",
    "PHASE 7: build Electron screens in src/renderer using src/renderer/src/lib/visaBackend.js — start with OCR-review + native-signature + Live-View-handoff + payment-modal + appointment-calendar.",
    "PHASE 10: implement app/storage.py (S3/GCS + KMS + signed URLs) behind capability flag; add data export/delete endpoints + tests.",
    "PHASE 11: add sentry-sdk + OpenTelemetry + /metrics + provider/adapter kill-switch flags.",
    "Wire native-signature into the workflow AUTHORIZATION_PENDING gate (currently sign endpoint sets envelope completed; connect to workflow.sign_authorization)."
  ]
}
