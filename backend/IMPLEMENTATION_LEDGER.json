{
  "updated": "2026-07-23 baseline reverification at 60bb9bf before global-snapshot brief",
  "baseline_2026_07_23_reverified": {
    "git": "main @ 60bb9bf, working tree clean",
    "backend_hermetic": "198 passed (pytest tests/ minus live_providers/live_temporal_restart/private_passports), 5.86s, ACTUALLY RERUN",
    "js_tests": "56 passed (npm test: 8 security + 14 visa+i18n + 34 portal), ACTUALLY RERUN",
    "renderer_build": "electron-vite build green, ACTUALLY RERUN",
    "docker": "8/8 services up, 7 with healthchecks all healthy (postgres, temporal, temporal-ui, minio, mailpit, api, worker, mock-portal), VERIFIED",
    "frontend_backend_acceptance": "1 passed against live stack (full applicant lifecycle via visaBackend.js), ACTUALLY RERUN",
    "release_secret_scan": "CLEAN on existing release/ (scan-release-secrets.sh), ACTUALLY RERUN",
    "electron_packaging": "build_security.test.mjs passed within npm test (deep asar scan of existing release); full clean rebuild deferred until after Trip.com-only conversion changes the bundle",
    "notes": "docs/PRODUCTION_BLOCKERS.md, data/, migrations/ do not exist yet — deliverables of the 2026-07-23 global-snapshot brief"
  },
  "completed_2026_07_23_sweep_review_fixes": [
    "Ran a 2nd adversarial review (35 agents, 5 dimensions) over the sweep diff 9fbdfd4..HEAD; 26 findings confirmed. ALL addressed + regression-tested (backend hermetic 191->198).",
    "FIX HIGH (gate bypass — 2 entry points): the 15-gate + passport-validity hard block lived ONLY in the /start HTTP handler, so POST /cases/{id}/signals/{name} AND the background worker (worker.tick_once) both bypassed it. CENTRALIZED both guards into service.enforce_safety(), called at the single durable transition point service.signal — every caller (start, signals, worker) now enforces. HTTP handlers translate PreparationOnlyMode + passport_validity.PassportBlocked into 409s via _signal_or_gate_error. Tests: signals-path-blocked, worker-does-not-start-blocked-case. (Test fixture used an EXPIRED specimen expiry 120415 → updated _new_case to 330415; the block was working, the fixture was unrealistic.)",
    "FIX HIGH (passport honesty): check_case_passport claimed 'satisfies the verified rule' when travel dates were missing so the rule was never evaluated → now returns status ok_pending_travel_dates honestly.",
    "FIX HIGH (no column migration): db.create_all never ALTERs existing tables, so added columns (EmailNotification.status/event/locale/attempts/last_error, StoredDocument.execution_class/page_classification) would be missing on an upgraded DB. Added _ensure_columns() idempotent additive migration (introspect + ADD COLUMN with defaults, SQLite+Postgres, never fails startup; Alembic remains authoritative for hard changes).",
    "FIX HIGH (erasure gap): DocumentBlob (raw passport-scan bytes, keyed by document_id) was not deleted by applicant erasure → now deleted in privacy.delete_case. Test: erasure-deletes-blob.",
    "FIX MED: personal_gate now BLOCKS MANUAL_REVIEW_REQUIRED/UNSUPPORTED classes (fail-safe for unknown/undeclared drivers, not fail-open). config.app_base_url (ELLIS_APP_BASE_URL) added so email deep links aren't a hardcoded placeholder. init_otel reports 'configured (exporter not wired)' instead of a false 'enabled'. rules.route_status: no rung-skipping (fees_verified before handoff), adapter counts only for meaningful lifecycle states + requires verified rule+fee, deterministic adapter selection (order by version/created_at), paused/killed → temporarily_paused. observability.scrub now catches all-numeric passport numbers + signed URL params (sig/token/t). /routes/readiness redacts gate evidence + admin id for non-admins.",
    "FIX LOW: email process_queue 'processed' counts only rows actually attempted (not tenant-skipped); tripcom process_deliveries retries 'unconfigured' rows after config arrives; SandboxClient nonce is now random (uuid) so a retry + admin replay of an identical payload in the same second get distinct nonces; passport enforce_and_notify docstring corrected. Regression tests added across test_personal_gate, test_doc_preview, test_tripcom_admin, test_errors_observability.",
    "REFUTED/left: route-key self-attestation (#8) is advisory (cross_document_conflicts already flags nationality mismatch; no live routes today); several dup findings folded into the above."
  ],
  "completed_2026_07_23_full_task_sweep": [
    "PERSONAL-TEST GATE (brief #6): app/personal_gate.py + models.RouteReadiness — the 15 gates VERBATIM, admin-only set_gate with mandatory evidence, REQUIRED_APPLICANT_INFO schema, live_preflight per case, assert_ready_for_live_action hard-blocks POST /cases/{id}/start for LIVE_SANDBOX/LIVE_PRODUCTION routes until every gate + all applicant info complete (mock/local proceed, labeled). Endpoints /routes/readiness + /admin/routes/readiness + /cases/{id}/live-preflight. tests/test_personal_gate.py 9.",
    "PHASE 2 RULES: app/rules.py + models.RouteRule — versioned official-source rules (source_url mandatory), review pending→verified/rejected/stale (pending NEVER served as verified), version chain w/ superseded_by, passport_validity_rule per destination. route_status/coverage_matrix use the brief's honest ladder (not_researched→…→production_approved; paused/killed adapters → temporarily_paused; a row's existence is never a verification). Endpoints /admin/routes/rules(+review), /routes/rules, /routes/coverage.",
    "PHASE 3 FEES: app/fees.py + models.FeeRecord — versioned fees from official sources only, review-gated, verified_current_fee STALE after 90d (FEE_MAX_AGE_DAYS), fee_breakdown honest when absent (automated_payment_allowed=False), stale_fees admin alert. Kimi never invents a fee. Endpoints /admin/routes/fees(+review,+stale), /routes/fees. tests/test_rules_fees.py 10.",
    "PHASE 5 VALIDITY: app/passport_validity.py — MRZ YYMMDD + ISO parsing, expired ⇒ start blocked (409) with explanation + OFFICIAL renewal authority per issuing country (RENEWAL_AUTHORITIES, honest fallback) + idempotent queued email + 'upload renewed passport and try again' recovery; destination-specific rules (valid_on_arrival/through_departure/months_after_arrival|departure/min_blank_pages) ONLY from the VERIFIED route rule — never a generic 6-month rule; unverified rule ⇒ flagged not guessed. Wired into approve_document + start_case + live-preflight + /cases/{id}/passport-validity.",
    "PHASE 8 EMAIL: app/emails.py — all 19 brief events templated in en + zh-CN + explicitly-maintained zh-Hant; SensitiveContentError guard (passport numbers/MRZ/Live-View URLs refused); signed expiring deep links (verify_deep_link); EmailNotification gains status/event/locale/attempts/last_error; process_queue retry→dead-letter (MAX_ATTEMPTS=3, never silently dropped), local recorder honestly 'recorded' not 'sent'; sender resolved from TenantSetup (Phase 7) or local. Endpoints /cases/{id}/emails, /admin/email/process-queue, /admin/email/dead-letters. tests/test_emails_validity.py 14.",
    "PHASE 13 PREVIEW: models.DocumentBlob stores upload bytes; GET /cases/{id}/documents/{doc_id}/url (auth+tenant) mints 300s signed URLs; GET /documents/{doc_id}/content serves via signature only (for img/iframe), private no-store; text-only fixtures honestly 'unavailable'. Renderer DocPreview.jsx (zoom/rotate/PDF iframe/download/loading/error) wired into OcrReview + visaBackend.documentPreviewUrl. tests/test_doc_preview.py 5.",
    "PHASE 14 ERRORS: app/provider_errors.py — 22-category taxonomy (Kimi token/quota/rate/auth/unavailable, DocAI, Browserbase, email, storage, temporal, postgres, container, disk, memory, worker, portal timeout/maintenance/rate; Docker has no AI tokens) each with safe user_message + data_preserved/retry/manual_review/provider_status/admin_alert; redact_diagnostic scrubs keys/JWTs/URLs; CircuitBreaker (threshold/cooldown/half-open + retry budget window, injectable clock) + capped exponential backoff + process registry; GET /diagnostics/providers. tests/test_errors_observability.py 11.",
    "PHASE 17 OBSERVABILITY: app/observability.py — deep scrub() (sensitive keys + value shapes: provider keys, bearer/JWTs, AWS, vault refs, Live-View URLs, MRZ, passport-like ids), log_event structured+scrubbed, RequestLogMiddleware (method/path/status/duration ONLY — no bodies/query strings) wired into the app, Sentry/OTel init honestly 'disabled' without DSN/deps (never fake-enabled), status in /diagnostics/providers. Redaction proven by tests.",
    "PHASE 15 PARTIAL: app/integrations/tripcom_admin.py + models.WebhookDelivery — persistent outbound deliveries, HMAC-signed per sandbox contract from the vaulted tenant webhook secret, retry→dead-letter, admin REPLAY (clone, original immutable), honest 'unconfigured' without endpoint+secret, health with 'local sandbox contract — NOT Trip.com production' label + fingerprints only; case completion queues case.status carrying execution_class + is_real_government_result (Trip.com can never mistake MOCK for real). Endpoints /admin/tripcom/{health,deliveries,replay,process}. tests/test_tripcom_admin.py 6.",
    "PHASE 19 DOCS: README_TRIPCOM_TESTING.md (execution-classification note + 8a new-feature walkthrough + updated baselines) + TRIPCOM_ACCEPTANCE_TEST_PLAN.md AT-28..AT-40 + honest gaps (no live portal, no cloud staging, load tests NOT run, demo recording pending, connector not production)."
  ],
  "completed_2026_07_23_adversarial_review_fixes": [
    "Ran an adversarial multi-agent review (18 agents, 5 dimensions -> per-finding verification) over the session diff 567fb93..HEAD; 8 findings confirmed after verification. ALL FIXED + regression-tested (backend hermetic 123->137).",
    "FIX CRITICAL (passport_classifier): _detect_mrz_kind searched the CONCATENATED both-MRZ-lines with the TD3 'P' pattern before MRV/TD1, so a 'P' in a visa/ID name zone (PETROV/PHAM) made a machine-readable VISA classify as a passport and be accepted as the identity source. Now reads the ICAO document code from the START of MRZ line 1 (_mrz_lines) with an earliest-match fallback for merged OCR. main.py add_document now clears extracted identity fields for ALL rejected pages (incl. passport_biodata_unverified) so no rejected/visa/ID page can seed identity. Tests: visa+P-name, TD1+P-name, endpoint visa/unverified store no identity.",
    "FIX HIGH (execution.classify_adapter): (1) an undeclared/unknown driver (not a provider class) was ELEVATED to LIVE_PRODUCTION by a config claim because clamp passed the claim through — now returns the driver's disposition (never real) when driver_ec not in PROVIDER_CLASSES. (2) an approved-but-not-enabled/paused adapter with a live driver still returned LIVE_PRODUCTION — now capped at LIVE_SANDBOX via _cap() (honors 'at most LIVE_SANDBOX when not enabled'). Runtime unaffected (MockPortal driver still clamps everything to MOCK). Tests: undeclared-driver-not-real, live-not-enabled=sandbox, live-approved-enabled=real.",
    "FIX (record_execution): document_ocr was stamped is_real_government_result=True on a live OCR run (category error — OCR is not a government outcome). Added government_outcome param (default False); only case_completed passes True. Tests: ocr-record-not-real, government_outcome flag.",
    "FIX (i18n masking): identifier regex missed purely-numeric passport numbers (US 9-digit) and lowercase alnum ids — added a numeric_id pattern (\\d{7,}) + case-insensitive alnum branch; sentinel generation now picks a marker guaranteed absent from the input (no restore collision). Tests: numeric-passport-masked, lowercase-id-masked, sentinel-collision-safe.",
    "FIX (setup config leak): free-form nested config dicts (google/trip/base_urls/branding) are now sanitized (_sanitize_config strips secret/password/token/api_key/credential keys) before storage, so a client can't nest a secret into non-secret config that redacted_status returns. Test: nested-secret-stripped.",
    "FIX (discovery domain spoof): classify_domain treated a 'gov' label in ANY position as government (visa.gov.com/fastvisa.gov.io => government). Now requires a proper suffix match (host.endswith(tld)). Test: gov-label spoofs rejected, real gov suffixes still pass.",
    "REFUTED (not changed): email _redact truncation (no triggerable leak), get_case adapter_verified default (can't produce a mock-as-real leak), and the discovery domain finding's test-coverage FRAMING (the underlying logic weakness WAS fixed above)."
  ],
  "completed_2026_07_23_phase7_setup": [
    "PHASE 7 Trip.com first-run administrator setup. NEW app/setup.py + models.TenantSetup (stores NON-SECRET config + per-component vault ref + redacted sha256[:12] fingerprint; a saved credential is NEVER returned again). save_setup validates that an email sender ADDRESS ALONE is rejected (SMTP/API credentials required); vaults kimi/browserbase/google-SA/email/trip-client/trip-webhook secrets (backend-only); computes setup_complete + missing[]. test_component (connection/readiness, redacted), send_test_email (reports REAL delivery only — honest 'no_transactional_provider'/local-never-delivered, never fabricates), rotate_component, revoke_component. production_preflight() blocks default ELLIS_DEV_TOKEN/ADMIN_TOKEN/VAULT_PASSPHRASE/ACTION_SECRET + missing Clerk in production; wired into /readyz (503 on default creds in prod).",
    "NEW app/providers/email.py: EmailConfig + config_valid (sender alone insufficient) + LocalEmailProvider (dev recorder, never claims delivery) + SmtpEmailProvider (real smtplib verify+send) + ApiEmailProvider (generic HTTP) + get_provider. Redacted errors only. Endpoints (admin-gated): GET /setup/status, POST /setup, POST /setup/test/{component}, POST /setup/email/test, POST /setup/rotate/{component}, POST /setup/revoke/{component}. main.py now imports require_admin.",
    "FRONTEND screens/SetupWizard.jsx (admin): tenant/admin/data-region/retention, Kimi/Browserbase/Google, transactional email (provider+sender+reply-to+SMTP/API + credential), Trip.com base URLs+client id/secret+webhook secret. Secret inputs are write-only password fields, NEVER prefilled; after save only a 'configured · <fingerprint>' chip shows. Connection tests + Send-test-email + revoke buttons. Wired into App + Sidebar nav ('nav.setup' in all 3 locales) + visaBackend.js client methods (setup + i18n). backend/.env.example documents email + production-preflight vars.",
    "TESTS: tests/test_setup.py (10: status-before, non-admin 403, sender-alone rejected, smtp-needs-creds, secrets vaulted+never-echoed (value absent from response/DB/status), full-valid-complete, test+rotate(fingerprint changes)+revoke, send-test-email honest-without-provider, local-never-claims-delivery, production-preflight blocks defaults). Backend hermetic 113->123; npm still 56; renderer build green."
  ],
  "completed_2026_07_23_phase6_i18n_identity": [
    "PHASE 6 Mandarin toggle + Kimi translation + Ellis identity. BACKEND app/i18n.py: SUPPORTED_LANGS en/zh-CN/zh-Hant. protect_tokens/restore_tokens MASK placeholders {x}, URLs, emails, filenames, MRZ '<' runs, dates, amounts, and passport-number/identifier tokens (letters+digits) with opaque ⟦T…⟧ sentinels before any model call and restore byte-for-byte — identifiers/passport data are NEVER translated. translate() caches repeats, is honest ('unavailable' returns the ORIGINAL, never fabricated) when live Kimi is absent, passthrough for same-language, unsupported_language guard; translator is injectable for tests. ELLIS_SYSTEM_IDENTITY + is_identity_question (EN/简/繁) + assistant_identity_answer (Ellis, never Kimi/Moonshot, never gov-official/lawyer/embassy, in every language).",
    "WIRING: LiveKimiProvider._chat now PREFIXES ELLIS_SYSTEM_IDENTITY to every system prompt (model can never present as Kimi/Moonshot or an official). LiveKimiProvider.translate (backend-only, preserves sentinels). Endpoints: GET /i18n/languages, POST /i18n/translate (backend-only), GET /assistant/identity?lang.",
    "FRONTEND: lib/i18n.js (STRINGS for en/zh-CN/zh-Hant with identical key sets, t() with {var} interpolation + en→key fallback) + lib/locale.jsx (LocaleProvider persisting to localStorage, useLocale/useT, visible LanguageToggle EN/简/繁). App wraps everything in LocaleProvider; Sidebar nav labels + section labels + language toggle localized. electron-vite build green.",
    "TESTS: backend tests/test_i18n.py (12: protect/restore roundtrip, identifiers/URLs preserved through translate, cache, unsupported/passthrough, honest-unavailable, identity-question detection EN/简/繁, identity-answer=Ellis-not-model in all langs, system-prompt forbids disclosure+official claims, /i18n/languages, /assistant/identity, /i18n/translate unavailable). JS tests/visa/i18n.test.mjs (8: locale parity/no-blank, t() localize+fallback+interpolate, identity mentions Ellis not model, localized disclaimers). Backend hermetic 101->113; npm 48->56."
  ],
  "completed_2026_07_23_phase1_portal_finder": [
    "PHASE 1 official-portal finder completed: app/portal/discovery.py query_variants now emits EVERY required brief form ('[dest] embassy', '[dest] embassy tourist visa', 'official [dest] tourist visa application', 'official [dest] e-visa', '[dest] immigration tourist visa', '[dest] ministry of foreign affairs visa', '[dest] consulate tourist visa', 'authorized [dest] visa application center') plus nationality/residence-specific queries when known. NEVER trusts one result — only government-domain candidates are 'verified'; a known contractor still needs a human-recorded official linking source.",
    "EVIDENCE: the disabled draft now stores queries, candidates (url/title/excerpt<=500/source_hash/found_by + domain classification), verified_candidates, official_verification_urls, portal_operator, domain_evidence, verification_timestamp, effective/expiration_date placeholders, applicant nationality+residence, nationality_residence_rules placeholder, reviewer, review_status=pending_admin_review, portal_limitations, review_checklist. Still adapter_status=disabled_draft + production_enabled=False + requires_admin_review=True; Kimi can never activate.",
    "SEARCH PROVIDER: pluggable set_search_provider(fn)->[SearchResult]; absent one, _search raises SearchUnavailable and discovery is honestly 'unavailable' (no fabrication, no anti-bot bypass). A Browserbase key alone is NOT a provider — wiring the live scrape is a deliberate activation step.",
    "CHANGE DETECTION: PortalSnapshot.fingerprint() (url_hash+content_hash) + detect_portal_change(prev,cur) (material change = url moved or content changed) + scan_for_changes(monitored, fetcher=None): without an approved fetcher it reports monitoring_unavailable (no fabricated fetches); with one it returns the adapters to PAUSE for review. /discovery endpoint accepts nationality/residence and stamps verification_timestamp.",
    "TESTS: tests/test_discovery.py 5->12 (all required query forms, nationality/residence omitted-when-absent, full evidence fields, never-trust-first-result via fake provider, first-result-not-verified, change detection first-seen/identical/content/url, scan unavailable w/o fetcher, scan pauses changed adapter). Backend hermetic 94->101."
  ],
  "completed_2026_07_23_phase4_passport_classifier": [
    "PHASE 4 passport biodata-page classifier + visa/stamp rejection: NEW app/providers/passport_classifier.py. Deterministic PRIMARY signal = ICAO MRZ kind (TD3 passport vs MRV visa vs TD1/TD2 ID/residence — all require '<' filler runs so prose can't be mistaken for an MRZ) + keyword evidence. Accept ONLY a checksum-validated TD3 biodata page as the passport identity source. Reject with the EXACT brief message ('This appears to be a visa or stamp page, not the passport biodata page. Upload the page containing your photograph, passport number, personal information and machine-readable zone.') for visa_page + stamp_page; distinct guidance for national_id/residence_permit/observation_page/passport_cover/blank_page/unreadable/passport_biodata_unverified (TD3 present but checksum invalid). A biodata-labels guard prevents a real biodata page that merely mentions 'visa' from being flagged. Kimi vision doc_type is a non-authoritative hint only.",
    "WIRING: ocr.OcrResult gains a TRANSIENT recognized_text (excluded from as_dict(), never persisted/logged — OCR text is PII) populated by every provider; used only for in-request classification. Upload endpoint runs classify_page after OCR: a rejected passport-adjacent page is stored with doc_type=page_type and NO extracted identity fields (a visa sticker can never seed passport identity); returns page_type/accepted_as_passport_identity/rejected/message. StoredDocument.page_classification persists the verdict. Frontend OcrReview shows a wrong-page banner with the exact message + 'Upload the biodata page and try again'.",
    "TESTS: tests/test_passport_classifier.py (16: valid biodata accept; MRV visa reject w/ exact msg; visa-keywords reject; stamp reject; TD1 id/residence; residence keyword; observation; blank; unreadable image; cover; invalid-MRZ unverified; supporting-doc not-an-error; biodata-labels guard; endpoint accept-passport, reject-visa-stores-no-identity, supporting-doc-not-rejected). Backend hermetic 78->94. npm still 48."
  ],
  "completed_2026_07_23_execution_classification": [
    "ABSOLUTE REQUIREMENT — execution classification: NEW app/execution.py defines ExecutionClass (exactly the brief's 7: MOCK, LOCAL_PROVIDER, LIVE_SANDBOX, LIVE_PRODUCTION, APPLICANT_ACTION_REQUIRED, MANUAL_REVIEW_REQUIRED, UNSUPPORTED). Classification is DRIVER-DETERMINED and clamped: a result can never be classified more-real than the driver that produced it, so a MockPortal-backed adapter that CLAIMS production still classifies MOCK (structurally impossible to convert a mock into a production result). classify_driver/classify_adapter/classify_ocr + clamp + is_real_government_result (True only for LIVE_PRODUCTION) + result_disposition (the display guard) + assert_not_mock_in_production + legend + record_execution (persists to NEW models.ExecutionRecord + audit).",
    "WIRING: MockPortal.execution_class='MOCK' (truthful self-declaration). StoredDocument gains execution_class + page_classification columns. OCR endpoint classifies (LOCAL_PROVIDER with no cloud creds; LIVE_PRODUCTION for Document AI/Kimi vision), persists on the doc + ExecutionRecord + audit, returns execution_class. GET /cases/{id} returns execution_class + a disposition block (is_real_government_result/display_status/disclaimer) and stamps appointment/confirmation with is_real flags. /capabilities exposes the legend + runtime_portal_execution_class='MOCK' + production_mode. Case-completion records a case_completed ExecutionRecord (MOCK). config.production_mode (env in production|staging).",
    "FRONTEND DISPLAY GUARD: lib/visaSession.resultDisposition() mirrors the backend, FAIL-SAFE (missing disposition => NOT real). CaseFlow.jsx: persistent ExecutionBanner on the journey tab + ResultView refuses to render 'Application submitted'/'Reference number' as real unless disposition.is_real_government_result — otherwise labels 'Mock reference (not a real visa reference)' + disclaimer. This satisfies 'the production UI must refuse to display submitted/paid/booked/confirmed unless the approved live adapter verified it'.",
    "TESTS: tests/test_execution.py (17: enum shape, driver classification, clamp cannot upgrade mock->prod, adapter-claims-prod-but-mock-driver=>MOCK, OCR local vs live, only LIVE_PRODUCTION is real, completed-mock-never-real, assert guard, capabilities legend, OCR endpoint persists LOCAL_PROVIDER, get_case MOCK disposition, completed-case confirmation not-real + ExecutionRecord). tests/visa/visa_ui_logic.test.mjs +3 (mock not real, missing=>not real fail-safe, live-prod real). Backend hermetic 78 (was 61); npm 48 (was 45)."
  ],
  "resume": {
    "how_to_continue": "Everything needed is in this repo + ledger + tests + commit history. Recreate venv: (cd backend && python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt). Real creds live ONLY in the git-ignored backend/.env (not committed) and host ADC at ~/.config/gcloud/application_default_credentials.json. Private passport fixtures live ONLY at ~/.ellis_private_fixtures (0600, outside repo). Bring up the full stack: (cd backend && docker compose up -d) — 8 services, all healthy. Run the app: npm run dev.",
    "done_this_session": "NEW-BRIEF 2026-07-23 (checkpoint 0fd13a6; incl. 2 adversarial reviews + all fixes): (1) ABSOLUTE REQUIREMENT execution classification — app/execution.py, MOCK never presented as real, driver-clamped, persisted+display-guarded (commit 870e242). (2) Phase 4 passport biodata classifier + visa/stamp rejection with exact message (commit 8c81edc). (3) Phase 1 official-portal finder — all query variants + full evidence + pluggable search + change-detection (commit 0a4a740). (4) Phase 6 Mandarin toggle + backend Kimi translation (token/identifier-preserving, honest-unavailable) + Ellis identity guard (commit 850534f). (5) Phase 7 Trip.com first-run setup wizard — vaulted backend-only creds, never echoed, sender-alone rejected, test/rotate/revoke, production_preflight gates /readyz (commit df7bf46). Backend hermetic 61->123, npm 45->56.",
    "next_implementation_task": "Remaining (in priority order): (1) real official-portal discovery ACTIVATION — register a controlled search provider (Browserbase-driven or approved web-search API) behind discovery.set_search_provider and run real discovery for the personal route, then admin-verify rules+fees+gates for that exact route; (2) Phase 16 cloud staging deploy (needs real infra + secrets via Secret Manager — cannot be done from this machine alone); (3) Phase 9 real Playwright portal drivers per approved adapter (blocked on route approval); (4) Phase 18 full graphical demo recording + configurable load tests; (5) Trip.com real connector once Trip.com supplies specs/credentials (tripcom_admin.health stays is_tripcom_production=false until contract tests pass against their env); (6) renderer screens for SetupWizard-adjacent admin views (route gates, coverage, tripcom deliveries) — backend endpoints all exist.",
    "next_file_to_modify": "backend/app/portal/discovery.py (register a real search provider via set_search_provider — activation step) OR src/renderer/src/screens/AdminConsole.jsx (add Route Readiness + Coverage + Tripcom deliveries tabs reading the new endpoints).",
    "next_command_to_run": "cd backend && . .venv/bin/activate && python -m pytest tests/ -q --ignore=tests/test_live_providers.py --ignore=tests/test_live_temporal_restart.py --ignore=tests/test_private_passports.py   # 191 pass (order-independent); npm test => 56",
    "verify_commands": [
      "cd backend && . .venv/bin/activate && python -m pytest tests/test_unit.py tests/test_e2e.py tests/test_signature.py tests/test_discovery.py tests/test_tripcom.py tests/test_temporal.py tests/test_privacy.py -q   # 53 pass",
      "npm test   # 43 pass (6 security + 11 visa + 26 portal)",
      "cd backend && docker compose up -d && LIVE_TEMPORAL=1 .venv/bin/python -m pytest tests/test_live_temporal_restart.py -s   # live worker-restart durability",
      "node --test tests/visa/frontend_backend.test.mjs   # frontend->backend full lifecycle (needs stack up)",
      "cd backend && . .venv/bin/activate && python -m pytest tests/test_live_providers.py -q   # needs backend/.env creds",
      "cd backend && . .venv/bin/activate && ENABLE_PRIVATE_DOCUMENT_SMOKE_TESTS=true PRIVATE_PASSPORT_FIXTURE_US=~/.ellis_private_fixtures/fixture_us.jpg (+SG/SG_PDF/CN/CN_2/PDF) python -m pytest tests/test_private_passports.py -q"
    ]
  },
  "test_status": {
    "js_framework": {"suite": "node --test tests/portal/", "result": "26 passed"},
    "backend_full": {"suite": "pytest test_unit+test_e2e+test_signature+test_discovery+test_tripcom+test_temporal", "result": "45 passed"},
    "live_providers": {"suite": "pytest tests/test_live_providers.py", "result": "3 passed (kimi text, kimi vision, browserbase) — needs .env creds"},
    "private_passport_smoke": {"suite": "pytest tests/test_private_passports.py (flag-gated)", "result": "all 6 fixtures via Document AI OCR+Form Parser; 6/6 MRZ checksum valid, high confidence"}
  },
  "providers": {
    "kimi_k3":        {"status": "provider_tested",       "notes": "live text + vision; reasoning_content fallback + big token budget"},
    "google_document_ai": {"status": "provider_tested",  "notes": "ADC via google-auth; OCR + Form Parser HTTP 200; real passport MRZ checksum VALID; quota project quiet-terminal-503221-i3; processors in project 712137185724"},
    "browserbase":    {"status": "provider_tested",       "notes": "session create/release live; live_view_url wired"},
    "postgres":       {"status": "integration_tested",    "notes": "compose up healthy; schema applied clean; full workflow + restart durability on Postgres; BigInteger fix for epoch-ms"},
    "docker":         {"status": "integration_tested",    "notes": "FULL compose stack up + healthy: postgres, temporal(+ui), minio, mailpit, api, worker, mock-portal. Dockerfile drops tests COPY; requirements add psycopg[binary]+temporalio; every service has healthcheck + startup ordering; ADC mounted read-only into api+worker only. Containerized API creates cases on Postgres."},
    "stripe_issuing": {"status": "implemented_untested",  "notes": "applicant payment window is the working default"},
    "clerk":          {"status": "implemented_untested",  "notes": "dev-token auth active; verify_clerk stubbed"},
    "temporal":       {"status": "integration_tested",    "notes": "REAL Temporal cluster (temporalio/auto-setup 1.24) live in compose; worker (app.temporal_worker) connects with a DB-backed portal store (portal_states table) so reconciliation survives restart. tests/test_live_temporal_restart.py (LIVE_TEMPORAL=1) drives a case to a human-action state, STOPS the worker container (Temporal describe=RUNNING → durable), RESTARTS it, resumes via signals, completes, and proves charge/book/submit each == 1 across TWO restarts + reconcile short-circuit. In-process time-skipping tests still 2 pass."}
  },
  "ocr_hierarchy": {
    "tier1_document_ai": "primary, provider_tested on consented passport",
    "tier2_kimi_vision": "flag-gated ENABLE_KIMI_OCR_FALLBACK, image-only, records fallback, provider_tested reachability",
    "tier3_local_mrz": "deterministic ICAO parse+checksum, unit_tested",
    "conflict_policy": "checksum-valid deterministic MRZ preferred; disagreements -> applicant review (cross_document_conflicts)"
  },
  "features": {
    "case_api": "unit_tested", "document_upload_ocr": "provider_tested", "applicant_review_approve": "unit_tested",
    "durable_workflow": "integration_tested", "worker_restart_durability": "integration_tested",
    "kimi_tool_security": "unit_tested", "tenant_isolation": "unit_tested", "secret_redaction": "unit_tested",
    "appointment_engine": "unit_tested", "portal_adapters_mockland_vietnam": "unit_tested",
    "applicant_payment_window": "unit_tested (descriptor)", "ocr_health_diagnostic": "provider_tested"
  },
  "private_fixtures": {
    "location": "~/.ellis_private_fixtures (0700, files 0600, OUTSIDE repo, gitignored, dockerignored)",
    "recorded": "sha256 only; no personal values anywhere",
    "env_flags": ["ENABLE_PRIVATE_DOCUMENT_SMOKE_TESTS", "PRIVATE_PASSPORT_FIXTURE_PDF/US/SG/CN"]
  },
  "completed_2026_07_22b": [
    "PHASE 1 native e-signature: app/providers/esign.py (ESignatureProvider + NativeEllisSignatureProvider) + pdfgen.py (dep-free signed PDF) + models NativeSignature/SignatureEvent + /authorization/prepare + /authorization/sign (consent+intent+step-up token gates) + tamper-evident artifact hash + material-change invalidation + cross-tenant block. tests/test_signature.py 6 pass.",
    "PHASE 5 portal discovery: app/portal/discovery.py (query variants + official-domain classification + disabled-draft-only) + Kimi allowlist tool discover_official_visa_portal + models PortalDraft + /discovery + /discovery/drafts + review endpoint. tests/test_discovery.py 5 pass. Live Browserbase search = activation step (returns honest 'unavailable', never fabricates).",
    "PHASE 9 Trip.com sandbox: app/integrations/tripcom.py (HMAC signing ts.nonce.body, replay protection, idempotency, typed webhook payloads, SandboxClient, deep-link) + docs/TRIPCOM_REQUIREMENTS.json (machine-readable what-Tripcom-must-provide). tests/test_tripcom.py 6 pass."
  ],
  "test_totals_now": {"js": "56", "backend_hermetic": "198 (order-independent; incl. 2 adversarial-review passes fixed)", "live_providers": "3", "private_passport": "all 6 fixtures processed via Document AI, 6/6 MRZ checksum valid"},
  "completed_2026_07_22c": [
    "PRIVATE FIXTURES: all 6 consented passports (US/SG jpg+pdf, CN x2 jpg, PDF) processed through REAL Google Document AI (Enterprise OCR + Form Parser both ok on all 6); after MRZ parser hardening (regex search-anywhere in _find_td3_lines + _repair_numeric OCR fix for O/0/I/1 on date+checkdigit fields) all 6/6 classify as passport with deterministic ICAO MRZ checksum VALID, high confidence, 0 warnings, 0 conflicts. Structural-only reporting; no personal values. Chinese-passport MRZ (line1 'PO'+CHN, OCR noise) now validates.",
    "PHASE 2 REAL TEMPORAL: temporalio 1.30.0 installed (py3.14); app/temporal_app.py (VisaProcessingWorkflow @workflow.defn + 8 @activity.defn wrapping mock portal; signals approve_review/sign_authorization/solve_captcha/verify_email/approve_payment/complete_payment/complete_declaration; pay/book/submit activities RECONCILE via get_application_state before acting); app/temporal_worker.py entry point for TEMPORAL_HOST; docker-compose worker -> app.temporal_worker. tests/test_temporal.py 2 pass using WorkflowEnvironment.start_time_skipping(): drives to COMPLETED via signals, asserts no dup pay/book/submit, and Replayer.replay_workflow(history) proves determinism = restart-safe. pytest.ini asyncio_mode=auto."
  ],
  "completed_2026_07_23_security_phase1": [
    "SECURITY CORRECTION: removed the bundled Kimi key from the Electron client. Confirmed shipped inside release/mac-arm64/Ellis.app/Contents/Resources/app.asar (/resources/kimi.key) → marked for rotation in docs/SECURITY_ROTATION.md. src/main/index.js no longer reads any bundled/drop-in credential file; all client-side external-provider calls gated behind clientProviderCallsAllowed()=!app.isPackaged (packaged app = zero direct provider calls; everything backend-only via visaBackend.js). package.json build.files hardened with negation globs. Deleted leaked key + compromised release. Added tests/build_security.test.mjs (in npm test): fails on any provider key/private fixture/.env/ADC/credential resource in a shippable dir or inside a packaged app.asar. npm test 32 pass (6 security + 26 portal). Client error messages redact secrets.",
    "PHASE 1 LIVE DOCKER + TEMPORAL: full compose stack (postgres, temporal+ui, minio, mailpit, api, worker, mock-portal HTTP service app/portal/http_mock.py) with healthchecks + correct depends_on ordering; temporal healthcheck uses service-DNS tctl. DB-backed portal store (app/portal_store.py + models.PortalState) makes reconciliation restart-safe. Proved live worker stop/restart durability + no duplicate pay/book/submit via tests/test_live_temporal_restart.py."
  ],
  "completed_2026_07_23_applicant_ui": [
    "PHASE 2-7 APPLICANT UI: full graphical applicant workflow in the Electron renderer, wired to the backend via src/renderer/src/lib/visaBackend.js over authenticated HTTP (NEVER providers directly). New nav 'Visa Platform' -> screens/VisaConsole.jsx (session, new case, case list, honest capability + country matrix) mounting components/visa/CaseFlow.jsx which reads workflow status and resolves each backend handoff by signal. Components: OcrReview.jsx (drag/drop upload, MIME/size validation, Document-AI status, per-field accept/edit/reject with confidence+provenance+MRZ-conflict highlight, missing-info step, approve-canonical -> signature invalidation feedback), Preferences.jsx (full appointment prefs incl. auto-book/reschedule controls), handoffs.jsx (SignatureModal typed/drawn + consent/intent/step-up + document hash; LiveViewModal captcha/email/otp/identity — Ellis never solves CAPTCHAs, collects no passwords/OTP/card/CAPTCHA answers; PaymentApprove + PaymentModal applicant-controlled, card never seen; AppointmentCalendar slot pick; RescheduleConfirm; DeclarationModal — only the applicant signs). Pure logic in lib/visaSession.js.",
    "BACKEND UI-SUPPORT: CORS middleware (env ELLIS_CORS_ORIGINS) so the renderer can reach the API; getCase now returns appointment+confirmation; review returns required_fields/missing_fields/answers; new POST /cases/{id}/answers (missing-info, invalidates signatures); mock-only GET /cases/{id}/mock/verification for the email-verification demo (dev-gated, no PII).",
    "BUGFIX (durability): workflow._last_slots was not persisted across the per-signal reload, so APPOINTMENT_AVAILABLE falsely reported no availability after select_appointment when allowAutoBook=false. Now persisted in snapshot/restore. Surfaced by the new frontend->backend test.",
    "TESTS: tests/visa/visa_ui_logic.test.mjs (11 hermetic, in npm test) + tests/visa/frontend_backend.test.mjs (drives the REAL visaBackend.js client through create->upload->OCR->review->missing-info->preferences->prepare+sign->start->every handoff->COMPLETED against the live API; auto-skips if backend down). npm test now 43 pass (6 security + 11 visa + 26 portal); backend hermetic still 45; frontend->backend live PASS.",
    "GRAPHICAL DEMO: launched electron-vite dev (app boots clean: main+preload+renderer, no errors) and rendered the real Visa Platform screen in a browser via a safe window.ellis fallback (graceful degradation when the preload bridge is absent; never used in the real Electron app). Screens verified visually. Backend fetch from the preview sandbox is blocked (sandbox can't reach host localhost:8000) — that connectivity is proven instead by the host-side frontend->backend test."
  ],
  "completed_2026_07_23_privacy_ops": [
    "PHASE 10 PRIVACY: app/privacy.py + endpoints — GET /cases/{id}/export (portable secret-free bundle: case/applicant/documents/signatures/appointments/confirmations/audit), GET /export (tenant), DELETE /cases/{id} (case erasure: cascades every per-case table + portal_states + old audit, leaves a non-PII 'case_erased' tombstone; orphaned applicant erased), DELETE /applicants/{id} (right-to-erasure across all their cases). retention_due() lists cases past ELLIS_RETENTION_DAYS. UI: PrivacyPanel in the case Activity tab (export JSON download + confirm-gated erasure) + visaBackend client methods.",
    "PHASE 11 OPS: GET /readyz (API + DB reachability), GET /metrics (org-scoped, no PII: cases_by_state + appointment/submission/signature counts + capabilities + kill_switches), config.killswitches() (env ELLIS_KILL_KIMI/DOCUMENT_AI/BROWSERBASE/STRIPE/SUBMISSIONS + ELLIS_KILL_ADAPTERS) now gate the capability flags so an operator can disable a provider/adapter instantly without redeploy.",
    "TESTS: tests/test_privacy.py 8 pass (export completeness+secret-free, tenant-isolated export/delete 403, cascade+tombstone, applicant erasure, readyz, org-scoped metrics, kill-switch env). Backend hermetic now 53. Live smoke: /readyz + /metrics + frontend->backend regression all green."
  ],
  "completed_2026_07_23_brief3": [
    "NEW KIMI KEY: rotated Moonshot key placed ONLY in git-ignored backend/.env; verified FULLY working (real text + vision + provider-module calls) with no value printed/committed/packaged (fingerprint sha256[:12]=6b9bca47f632).",
    "BRIEF-3 PHASE 1 CLEAN BUILD: rm -rf out release; electron-vite build + electron-builder --dir; verified no resources/kimi.key, no credential/.env/ADC/fixture in app.asar, no secret VALUES in packaged contents, packaged main enforces backend-only (clientProviderCallsAllowed, 5 sites). scripts/scan-release-secrets.sh (extract+scan asar, redacted findings, CI exit code) + npm run scan:release. build_security.test.mjs strengthened (deep asar content scan + packaged-main guard + MOONSHOT/BROWSERBASE/LiveView patterns) → 8 tests. .github/workflows/security.yml release gate. docs/ARTIFACT_SECURITY_REPORT.md. commit b372358.",
    "BRIEF-3 PHASE 2 ADAPTER ADMIN LIFECYCLE: app/adapters_admin.py (states discovered→disabled_draft→technical_review→policy_review→mock_tested→staging_tested→approved→limited_rollout→production_active→paused/rolled_back; ALLOWED_TRANSITIONS; ACTIVATION_STATES human-admin-only + NO AI actor may activate; immutable audit per transition; production_enabled only in rollout states & forced off by kill switch; version snapshots + rollback; coverage_matrix with honest service levels). models AdapterRecord+AdapterVersion. security.Principal gains role + require_admin (admin granted only by ELLIS_ADMIN_TOKEN, dev default 'admin-token'). Endpoints /admin/adapters (CRUD/transition/kill/clear-kill/rollback/coverage). UI screens/AdminConsole.jsx (list, detail w/ lifecycle+versions+rollback+kill, review queue, coverage matrix, immutable audit) + nav. tests/test_adapters_admin.py 8 pass (lifecycle, illegal-transition, activation-requires-admin, AI-cannot-activate, kill, config-frozen-after-approval, rollback, coverage). backend hermetic now 61; npm test 45. commit acad255.",
    "BRIEF-3 PHASE 11 DELIVERY PACKAGE: README_TRIPCOM_TESTING.md (prereqs, one-command startup, dev login accounts admin/1234 + dev-token/admin-token, mock creds, full scenario, troubleshooting, shutdown, rotation, known limitations) + docs/TRIPCOM_ACCEPTANCE_TEST_PLAN.md (27 acceptance tests AT-01..AT-27 with id/preconditions/steps/expected/evidence/result/notes + honest gaps) + scripts/{setup-local,start-local,health-check,run-acceptance-tests,stop-local,reset-local,build-electron-clean,scan-release-secrets}.sh (fail-safe, never print secrets) + ELLIS_ADMIN_TOKEN in backend/.env.example. run-acceptance-tests.sh => ACCEPTANCE: PASS (npm 45, backend 61, frontend->backend live, release scan CLEAN). health-check.sh => all 6 services OK."
  ],
  "not_done_yet": [
    "BRIEF-3 PHASE 3 global portal runtime (Browserbase/Playwright/versioned drivers/Stagehand-Kimi-assist-only/Temporal/human-takeover/idempotency/reconciliation) — runtime skeleton exists (temporal_app + mock portal); real Playwright drivers per approved portal remain.",
    "BRIEF-3 PHASE 4 real adapter rollout pipeline (safe examples only until Trip.com priorities), PHASE 5 payments/appointments polish, PHASE 6 Trip.com admin UI + webhook viewer + health, PHASE 7 cloud deploy IaC (staging/prod), PHASE 8 Sentry+OTel+metrics+alerts+redaction tests, PHASE 9 load tests+report, PHASE 10 full graphical demo recording, PHASE 11 Trip.com IT delivery package (README_TRIPCOM_TESTING.md, scripts/*, acceptance plan).",
    "PHASE 3/4/5(UI)/7(UI)/8-of-second-brief: full Electron applicant UI screens (Live View modals, payment modal, appointment calendar, OCR-review UI, signature UI) wired to backend via src/renderer/src/lib/visaBackend.js — DONE in checkpoint 05c849f (superseded).",
    "PHASE 7 full country-adapter build pipeline beyond discovery drafts + Vietnam config (real Playwright drivers per portal)",
    "PHASE 9 production multi-tenant storage (S3/GCS/KMS/Secret Manager), data export/deletion endpoints",
    "PHASE 10 monitoring (Sentry/OTel/metrics/alerting/kill-switches), load tests, staging deploy",
    "Stripe Issuing live, synthetic passport-layout fixtures, Electron end-to-end UI tests"
  ],
  "next_actions": [
    "PHASE 2: add temporalio to requirements + docker-compose temporal/temporal-ui services; create app/temporal_workflows.py mirroring workflow._step as activities behind TEMPORAL_HOST; keep DB runner for unit tests; prove kill/restart no-dup with a real worker.",
    "PHASE 7: build Electron screens in src/renderer using src/renderer/src/lib/visaBackend.js — start with OCR-review + native-signature + Live-View-handoff + payment-modal + appointment-calendar.",
    "PHASE 10: implement app/storage.py (S3/GCS + KMS + signed URLs) behind capability flag; add data export/delete endpoints + tests.",
    "PHASE 11: add sentry-sdk + OpenTelemetry + /metrics + provider/adapter kill-switch flags.",
    "Wire native-signature into the workflow AUTHORIZATION_PENDING gate (currently sign endpoint sets envelope completed; connect to workflow.sign_authorization)."
  ]
}
