# Ellis resume file for the next AI agent (written 2026-09-09, 22:40 UTC)

Paste this file as your first message. It is self-contained. Read it fully before touching anything, then continue from "What to do next". Be concise with the owner, answer first, no em dashes, no semicolons in text a reader sees. Never claim 100% accuracy or completeness. Deployment is authorized. Do not message Trip.com or anyone else.

## 1. Where everything lives

```text
WORKSPACE     = /Users/sammynawaly/Documents/Codex/2026-09-09/files-mentioned-by-the-user-handoff20260909
ACTIVE_CLONE  = WORKSPACE/work/ellis            (git branch h1b-edition, HEAD 3e88c3a at hand-off)
ORIGINAL      = /Users/sammynawaly/Documents/ellis-app   (same branch, merged level with the clone, pushed to GitHub)
PYTHON        = /Users/sammynawaly/Documents/ellis-app/backend/.venv/bin/python3.14
SCRATCH_ME    = /private/tmp/claude-501/-Users-sammynawaly-Documents-ellis-app/8fe6d93b-7a7f-4fb1-8881-c769deea089e/scratchpad
SCRATCH_OLD   = /private/tmp/claude-501/-Users-sammynawaly-Documents-ellis-app/6aff7301-465f-4502-b6f9-1d42d4185a19/scratchpad
JOURNALS      = /Users/sammynawaly/.claude/projects/-Users-sammynawaly-Documents-ellis-app/<session>/subagents/workflows/<run>/journal.jsonl
MEMORY        = /Users/sammynawaly/.claude/projects/-Users-sammynawaly-Documents-ellis-app/memory/   (read MEMORY.md, then stations-release-2026-09-09.md, systemic-hardening-2026-09-09.md, continuation-2026-09-09-evening.md)
```

The clone's git "origin" is the ORIGINAL checkout, and the original's origin is GitHub (git@github.com:sn3136-del/ellis-app.git, public, dead rotated keys in history, never commit a live secret). Work in the clone, then fast-forward the original and push from it. The clone has an untracked `node_modules` symlink, never commit it.

Server: `ssh -o BatchMode=yes -i /Users/sammynawaly/.ssh/ellis_hetzner root@49.13.220.169`. App at /opt/ellis, backend service `ellis-backend`, freshness worker `ellis-freshness.service` and timer, database /var/lib/ellis/ellis.db, operator overrides /var/lib/ellis/operator_overrides.json, health http://127.0.0.1:8000/health, public API https://ellis-visa.com/api, console https://ellis-visa.com/#ops. NEVER `git pull` or `rsync --delete` on the server: its git checkout is stale and releases are installed as checksum-pinned file deltas. Do not print .env or keys.

Read-only QC access uses the public marker header `X-Ellis-Token: public-quality-control` (GET /database/records?limit=5000, GET /database/freshness). Writes that need an operator identity are done on the server through the app's own Python (see section 6).

## 2. What is deployed right now

Backend commit 3e88c3a, installed 2026-09-09 22:3x UTC by `work/deploy_backend_code_release_remote.py hardening20260910` (report: /root/ellis-backup-code-hardening20260910/deployment-report.json). Frontend 2c5ce76 (unchanged). Full suite at 3e88c3a: 4,130 passed, 7 skipped.

Releases today, in order: b9a6a18 (source scope), 01f3bc4 (Japan11 + JSON CAS + backlog continuation), 561d904 (general reviewed-batch converter, overlay registry, batch stations20260910 with 12 routes), 3e88c3a (invariant refinement, ESTA channels, quiet unreadable tickets).

Live metrics after 3e88c3a (work/records-after-hardening.json, outputs/ellis-metrics-after-hardening-2026-09-09.json):

| measure | value |
|---|---|
| product records / canonical routes | 1,420 / 946 |
| fillable-cell completeness | 92.0% |
| product rows fully complete | 59.7% |
| requirement source support | 69.8% |
| held product rows | 718 |
| integrity conflicts (routes) | 10 (was 30) |
| Trip.com 306-pair sample | 305 present, 216 served, 89 held, 1 missing (IDN to VNM, 14 vs 30 day conflict) |

Trip.com's four named routes verified live after 561d904: HKG to VNM eVisa 25/50 USD 90 days, MYS to RUS unified eVisa, THA to AUS subclass 600, IDN to KOR withheld consistently, one canonical key across date and residence variants.

## 3. Doctrine (do not break)

- One cached answer per route (`kimi_primary.cache_key`, `is_canonical_key`, `canonical_key`). Dates and residence never fork it. Document and transit variants are real.
- Verified facts ship as reviewed overlays built by `backend/scripts/convert_reviewed_general_batch.py` from a `general_reviewed_batch` whose every value has a literal quote from a captured destination-government page that names the passport. Overlays are listed in `data/database_seed/reviewed_overlays.json` and loaded with the seed. Do not hand-edit verified rows without a quote from the entry's own page.
- `kimi_primary.serve_time_invariants(guidance)` is the one contradiction check (generation and final merged answer). Since 3e88c3a: a visa-free verdict may list an optional priced visa beside an explicit free lane (zero fee or exemption-named product); the contradiction is a visa-free verdict whose every product costs money. A free permit on arrival or an arrival card filed online is not a visa application. ELECTRONIC_AUTHORIZATION_REQUIRED with channel not_required is a contradiction (an ESTA is applied for online). ELECTRONIC_AUTHORIZATION_REQUIRED with an evisa or paper_visa detail is a contradiction (an e-visa is a visa).
- Contradictions hold the answer from readers until a person verifies the verdict. Only a verified verdict releases a hold.
- A failed page read never erases a good check. A stale answer is never regenerated from model memory. Unreadable pages are sweep statistics, not queue tickets (set ELLIS_FILE_UNREADABLE_ISSUES=1 only if per-row tickets are wanted again).
- High confidence needs a verified verdict; today nothing is High, which is honest. Official government sources only. Never bypass a bot wall or CAPTCHA. Never invent a fee, stay or validity.

## 4. The release pipeline (exact commands)

Research drafts come from the workflow script `station-rest-source-review-part-*.js` (schema DRAFT: verdict with proof, route_field_proofs, products with proofs, sources_read) with `args.list` pointing at a JSON list of {key, file, label}; route files are made by the generator in SCRATCH_ME/routes3 and SCRATCH_OLD/routes2 (format: key, nationality, destination, purpose, document, held, products with current cells). Flips and exemptions get two skeptic votes.

```sh
# 1 merge drafts and votes (task output files, plain files, or journals) into one plain file
PYTHON SCRATCH_ME/normalize_workflow_output.py out.json <inputs...>
# 2 capture the cited pages locally (idempotent, keeps ok entries)
PYTHON WORKSPACE/work/capture_sources.py urls.json sources.json     # urls via assemble_batch.cited_urls(drafts)
# 3 assemble: only quotes literally on captured pages, only verdicts that survived both skeptics
PYTHON WORKSPACE/work/assemble_batch.py drafts.json sources.json ACTIVE_CLONE/data/database_seed/reviewed_general_batch_<ID>.json general-<ID> <route_dir>
#    then drop products whose current_name is "No visa needed" (the visa-free row has no product identity)
# 4 export production layers read-only for the batch keys
scp keys.json root@49.13.220.169:/root/general-<ID>-keys.json
ssh ... "cd /opt/ellis/backend && /opt/ellis/backend/.venv/bin/python /root/export_review_layers_remote.py /root/general-<ID>-keys.json" > layers.json
# 5 convert (from ACTIVE_CLONE/backend)
PYTHON -m scripts.convert_reviewed_general_batch batch.json layers.json overlay.json report.json manifest.json
#    add the overlay file name to data/database_seed/reviewed_overlays.json
# 6 tests, commit, pin, package, upload, install, verify
PYTHON -m pytest -q -p no:cacheprovider tests            # must be all green
git commit ...; write work/backend-general-<ID>-tested-revision.json {source_commit, backend_tests_passed, ...}
PYTHON work/prepare_backend_general_release.py <ID> backend-general-<ID>-tested-revision.json <deployed_base_commit>
scp -r work/backend-general-<ID>-payload root@49.13.220.169:/root/ellis-backend-general-<ID>
scp work/deploy_backend_general_release_remote.py root@49.13.220.169:/root/
ssh ... "cd /opt/ellis/backend && /opt/ellis/backend/.venv/bin/python /root/deploy_backend_general_release_remote.py <ID>"
PYTHON work/verify_tripcom_final.py outputs/<name>.json ; PYTHON work/snapshot_release_metrics.py records.json outputs/<name>.json
```

For code-only or seed-only releases use `work/prepare_backend_code_release.py` and `work/deploy_backend_code_release_remote.py` the same way (stage name backend-code-<ID>). The install helper refuses to run while the freshness worker is active, pauses cron and the timer, backs up the database, verifies base hashes, installs, checks the stores load, runs the integrity audit, restarts, health-checks and restores monitoring. Run it with cwd /opt/ellis/backend. A new release needs a new ID (existing backup dirs block reuse).

Converter rejections seen today: "evidence does not state this verdict for this nationality" (quote does not name the passport on the destination page), "Product verdict outside the vocabulary" (Taiwan TAC), "Missing or ambiguous current product identity" (synthetic visa-free product). A new overlay entry can override an earlier verified correction (MYS to RUS) or blank a scoped field (RUS to TWN arrival card); run the full suite and drop colliding rows.

## 5. What to do next, in order

1. **Batch 2 research (18 routes) was running when the budget ran out**: run wf_ccc9e64a-8b9 (journal under session 8fe6d93b). Routes: the 10 unresolved integrity conflicts (CHN to PAK, CHN to SAU, HKG to TJK, TWN to VNM, USA to BEN: ETA verdict over an e-visa detail, needs the real verdict; USA to GAB, ARE to AIA, GBR to VNM, PHL to VNM, CHN to MOZ: visa-free verdict with no free lane) plus the Vietnam 17 and Korea 6 routes (IND, AUS, CAN, KOR, ESP, RUS to VNM; PHL, IND to KOR). Route list SCRATCH_ME/args_batch2.json, route files SCRATCH_ME/routes3. Recover finished drafts from the journal with normalize_workflow_output.py, rerun the rest (workflows from another session cannot be resumed, only replayed from journals), then run the pipeline above as batch `stations20260911` against base 3e88c3a. The Vietnam 17 and Korea 6 detached drafts and captures in WORKSPACE/work/audits are corroboration and extra sources (vietnam17-reviewed-draft-2026-09-09.json, korea6-source-backed-draft-20260909.json, ellis-public-vietnam17-captures/, ellis-public-korea6-more/); their handoff notes list what must stay held (CAN generic product, TWN ETA vs eVisa, IND stay-only proof, GBR physical mission product, PHL multiple C-3, IND multiple C-3-9).
2. **Dispute adjudication (305 field disputes) was running**: run wf_c8a06608-078, script SCRATCH_ME/rule-on-disputes.js, input SCRATCH_ME/disputes_to_rule.json (ids, cache keys, proposals with page quotes). Each ruling is page_correct, record_correct, page_generic, unreadable or unsure, with two skeptics on page_correct. Apply rulings on the server through the app (section 6): page_correct with two unrefuted votes becomes an operator override written by `verified_overrides.append_operator_entry` and the issue moves to corrected then published; record_correct and page_generic are dismissed with the reason; unreadable and unsure stay open. Never bulk-dismiss.
3. **The 339 station routes never reviewed**: list SCRATCH_ME/args_remaining.json, 13 drafts already in journal wf_7a626a29-3a8. Same pipeline. Expect the converter to reject drafts whose quotes do not name the passport; ask the researchers for the list line that names the nationality plus the rule sentence.
4. **32 rejected drafts from batch 1** (reasons in WORKSPACE/work/audits/general-stations20260910-report.json): re-research only the quote, not the route.
5. Refresh metrics, verify the four Trip.com routes, update outputs and memory, push. Report exact numbers and remaining limits.

## 6. Operator writes on the server

Tokens changed; the old admin-token is invalid. For operator actions run Python on the server as the app: `cd /opt/ellis/backend && .venv/bin/python - <<EOF ... EOF` importing `app.db.SessionLocal`, `app.visa_snapshot.models.DatabaseIssueReport`, `app.visa_snapshot.verified_overrides.append_operator_entry(entry, guidance=cached.guidance)`. Back up first: `sqlite3` read-only connection `.backup` to /root/ellis-before-<what>-<date>.db (example used today: /root/ellis-before-dismiss-unreadable-20260909.db). Record resolved_by "ellis-review", a plain resolution sentence and resolved_at. Today 803 "source_unreadable" tickets were dismissed as a class this way; 339 real issues remain open (30 of them integrity, now 10 after the hardening release; the freshness sweep will re-audit).

## 7. Files that prove the work

- WORKSPACE/outputs/ellis-stations-release-2026-09-09.json (batch 1 release, before and after metrics)
- WORKSPACE/outputs/ellis-metrics-after-hardening-2026-09-09.json
- WORKSPACE/outputs/ellis-tripcom-recheck-after-stations-2026-09-09.json
- WORKSPACE/outputs/ELLIS-CLAUDE-HANDOFF-2026-09-09.md (the long engineering handoff, still accurate for sections 3, 6, 9, 12)
- WORKSPACE/work/critical-release-checkpoint.md
- /root/ellis-backup-general-stations20260910 and /root/ellis-backup-code-hardening20260910 on the server (deployment reports, file backups, database backups)
- This repo: docs/handoff/HANDOFF-2026-09-09.md (older engine-phase notes) and this file.
